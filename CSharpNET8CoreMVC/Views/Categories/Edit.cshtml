@model Category

@section Title {
    @if (Model != null)
    {
        <h3>Category: @Model.Name</h3>
    }
}

@if (Model == null)
{
    <p>Select a category to edit.</p>
    return;
}

<partial name="_Category" model="@Model" />